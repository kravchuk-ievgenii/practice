<h1>Doubtful posts</h1>

<table>
  <thead>
    <tr>
      <th>Posts</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @posts.each do |post| %>
      <tr>
        <%  if post.content == "sheep" %>
        <td><%= post.content %></td>
        <td><%= post.user_id %></td>
      </tr>
    <% end %>
    <% end %>
  </tbody>
</table>

<br>
<%= link_to 'Back', user_posts_path %>